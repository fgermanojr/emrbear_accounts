%h3 Users List
= link_to 'New User', '/login/new', remote: true
%br
%br
-# Sample ruby comment
- @users.each do |user|
  = user.name
  = '*' if !current_user.nil? && user.name == current_user.name
  %br

