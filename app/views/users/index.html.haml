%h3 Users List
= link_to 'New User', '/login/new', remote: true
-# Sample ruby comment
- @users.each do |user|
  %br
  = annotate_user(user)
  - user.accounts.each do |account|
    %ul
      %li= annotate_account(user, account)
