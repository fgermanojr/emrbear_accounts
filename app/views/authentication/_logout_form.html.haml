= content_for :modal_header do
  User Logout
= content_for :modal_body do
  #user_logout_form
  = bootstrap_form_for('authenticate', url: '/logout', remote: true) do |f|
    = f.text_field :name, id: "user_name", label: "User Name:", value: current_user_display
    = f.submit 'Logout', class:'right'
= content_for :modal_footer do
  emrBear&trade;